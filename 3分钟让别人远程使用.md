# 3 分钟让别人远程使用 🚀

## 🎯 效果
- ✅ 代码在你电脑上跑
- ✅ 别人通过网址访问
- ✅ 对方不需要安装任何东西
- ✅ 你的电脑开着就能用

---

## 方法 1：ngrok（最简单）⭐⭐⭐⭐⭐

### 第 1 步：安装 ngrok（1 分钟）

```bash
brew install ngrok
```

### 第 2 步：注册并配置（1 分钟）

1. 访问：https://dashboard.ngrok.com/signup
2. 注册账号（免费）
3. 复制你的 authtoken
4. 运行：
   ```bash
   ngrok config add-authtoken 你的token
   ```

### 第 3 步：启动应用（1 分钟）

```bash
# 终端 1：启动前端
npm run dev

# 终端 2：启动 ngrok
ngrok http 3000
```

### 第 4 步：分享网址

ngrok 会显示：
```
Forwarding  https://abc123.ngrok-free.app -> http://localhost:3000
```

把这个 `https://abc123.ngrok-free.app` 发给别人就行了！

---

## 方法 2：Cloudflare Tunnel（更稳定）⭐⭐⭐⭐

### 第 1 步：安装

```bash
brew install cloudflare/cloudflare/cloudflared
```

### 第 2 步：登录

```bash
cloudflared tunnel login
```

### 第 3 步：启动

```bash
# 终端 1：启动前端
npm run dev

# 终端 2：启动隧道
cloudflared tunnel --url http://localhost:3000
```

会显示类似：
```
https://abc-def.trycloudflare.com
```

分享这个网址即可！

---

## 🎁 一键启动脚本

我已经创建了自动化脚本：

### 使用 ngrok 脚本

```bash
./远程访问-ngrok.sh
```

这个脚本会自动：
- ✅ 检查 ngrok 是否安装
- ✅ 启动前端服务
- ✅ 启动 ngrok 隧道
- ✅ 显示访问地址
- ✅ 按 Ctrl+C 自动清理

---

## 📋 完整流程示例

### 使用 ngrok：

```bash
# 1. 安装（只需一次）
brew install ngrok

# 2. 配置（只需一次）
ngrok config add-authtoken 你的token

# 3. 启动应用
npm run dev

# 4. 新开终端，启动 ngrok
ngrok http 3000

# 5. 复制显示的网址
# 例如：https://abc123.ngrok-free.app

# 6. 发给别人
# 对方在浏览器打开就能用了！
```

---

## 🔧 如果需要后端 API

### 方法 1：同时暴露后端

```bash
# 终端 1：前端
npm run dev

# 终端 2：后端
cd backend
python -m uvicorn main:app --reload --port 8000

# 终端 3：暴露前端
ngrok http 3000

# 终端 4：暴露后端
ngrok http 8000
```

### 方法 2：使用 ngrok 配置文件

创建 `ngrok.yml`：
```yaml
version: "2"
authtoken: 你的token
tunnels:
  frontend:
    proto: http
    addr: 3000
  backend:
    proto: http
    addr: 8000
```

启动：
```bash
ngrok start --all --config ngrok.yml
```

---

## 💡 常见问题

### Q: 每次地址都不一样？
A: 免费版每次启动地址会变。付费版可以固定域名。

### Q: 速度慢吗？
A: 一般很快，取决于你的网络。Cloudflare Tunnel 通常更快。

### Q: 安全吗？
A: 
- ngrok 和 Cloudflare 都是大公司，相对安全
- 数据会经过他们的服务器
- 如果需要更高安全性，用 Tailscale

### Q: 需要一直开着电脑吗？
A: 是的，关机后别人就访问不了了。如果需要 24/7 运行，考虑部署到云服务器。

### Q: 免费吗？
A: 
- ngrok：免费版有限制（每次地址变化，有连接数限制）
- Cloudflare Tunnel：完全免费
- Tailscale：免费版最多 100 台设备

---

## 🎯 推荐方案

### 临时演示（几小时）
→ **ngrok**
```bash
npm run dev
ngrok http 3000
```

### 经常使用（几天/几周）
→ **Cloudflare Tunnel**
```bash
npm run dev
cloudflared tunnel --url http://localhost:3000
```

### 长期使用（几个月）
→ **部署到云服务器**
- 参考 `让别人也能使用-部署方案.md`
- 或使用 Docker 部署

---

## 🚀 现在就试试！

### 最快的方法（如果已安装 ngrok）：

```bash
# 1. 启动应用
npm run dev

# 2. 新终端运行
ngrok http 3000

# 3. 复制网址发给别人
```

### 使用自动化脚本：

```bash
./远程访问-ngrok.sh
```

---

## 📚 相关文档

- `远程访问方案-代码在你电脑.md` - 详细说明
- `让别人也能使用-部署方案.md` - 云服务器部署
- `DOCKER_DEPLOYMENT.md` - Docker 部署

---

**总结：安装 ngrok → 启动应用 → 运行 `ngrok http 3000` → 分享网址！** 🎉
