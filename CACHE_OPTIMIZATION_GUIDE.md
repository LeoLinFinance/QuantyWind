# 缓存优化功能使用指南

## 功能概述

为了减少token消耗和提升响应速度，系统已实现智能缓存机制。现在数据刷新分为两种模式：

### 1. 智能缓存模式（默认）
- 自动使用缓存数据，避免频繁调用AI API
- 适用于：页面切换、自动刷新、添加/删除股票等日常操作

### 2. 强制刷新模式
- 忽略缓存，获取最新数据
- 适用于：手动点击刷新按钮、更新系统提示词后

## 缓存策略

### 市场洞察数据
- **缓存有效期**：5分钟
- **包含内容**：市场整体情绪、三大指数数据
- **刷新时机**：
  - 缓存过期后自动刷新
  - 手动点击"🔄 刷新"按钮强制刷新

### 股票基础数据
- **缓存有效期**：1分钟
- **包含内容**：价格、涨跌幅、成交量
- **刷新时机**：
  - 缓存过期后自动刷新
  - 手动点击"🔄 刷新"按钮强制刷新

### AI舆情分析
- **缓存有效期**：15分钟
- **包含内容**：每只股票的AI舆情分析
- **刷新时机**：
  - 缓存过期后自动刷新
  - 手动点击"🤖 更新舆情"按钮强制刷新
  - 更新系统提示词后强制刷新

## 使用建议

### 日常使用
1. **关闭自动刷新**（默认已关闭）
   - 页面右上角的"自动刷新"开关保持关闭状态
   - 避免每15分钟自动消耗token

2. **按需手动刷新**
   - 需要最新数据时，点击"🔄 刷新"按钮
   - 只想更新舆情时，点击"🤖 更新舆情"按钮

3. **利用缓存**
   - 页面切换时自动使用缓存数据，无需重新加载
   - 添加/删除股票时自动使用缓存，响应更快

### 特殊场景

#### 场景1：市场波动剧烈时
- 打开"自动刷新"开关
- 系统每15分钟自动更新（使用缓存，减少token消耗）
- 需要最新数据时手动点击刷新按钮

#### 场景2：调整AI提示词后
- 保存提示词后会自动强制刷新
- 清除所有AI舆情缓存，重新分析

#### 场景3：长时间未使用
- 首次打开页面会使用缓存（如果有效）
- 缓存过期会自动获取最新数据

## Token消耗对比

### 优化前
- 每次刷新：调用AI API分析所有股票
- 自动刷新（15分钟）：每次都消耗token
- 页面切换：重新加载所有数据

### 优化后
- 首次加载：调用AI API（正常消耗）
- 缓存有效期内：0 token消耗
- 自动刷新：仅在缓存过期时消耗token
- 页面切换：使用缓存，0 token消耗

### 预期节省
- **日常使用**：节省80%以上token
- **频繁切换页面**：节省95%以上token
- **自动刷新开启**：节省60%以上token

## 技术细节

### 后端缓存实现
```python
# 市场洞察缓存：5分钟
self._market_insight_cache = None
self._market_insight_cache_time = None

# 盯盘列表缓存：1分钟（基础数据）/ 15分钟（AI分析）
self._watchlist_cache = {}

# 单只股票舆情缓存：15分钟
self._sentiment_cache = {}
```

### API参数
```
GET /api/market-insight?force_refresh=true
GET /api/watchlist?use_ai=true&force_refresh=true
```

- `force_refresh=false`（默认）：使用缓存
- `force_refresh=true`：强制刷新

## 监控与调试

### 查看缓存状态
后端日志会显示缓存使用情况：
```
✅ 使用市场洞察缓存（缓存年龄: 120秒）
✅ 使用盯盘列表缓存（use_ai=True, 缓存年龄: 300秒）
✅ 使用 AAPL 舆情缓存（缓存年龄: 600秒）
🔄 刷新市场洞察数据...
🤖 对 3 只股票进行AI舆情分析...
```

### 前端日志
```
市场洞察页面：自动刷新触发（使用缓存）
```

## 常见问题

### Q1: 为什么数据没有更新？
A: 检查缓存是否还在有效期内。如需最新数据，点击"🔄 刷新"按钮强制更新。

### Q2: 自动刷新还会消耗token吗？
A: 会，但大幅减少。只有在缓存过期时才会调用AI API，其他时间使用缓存数据。

### Q3: 如何完全避免token消耗？
A: 关闭"自动刷新"开关，只在需要时手动点击刷新按钮。

### Q4: 缓存会影响数据准确性吗？
A: 不会。股票价格等关键数据缓存时间很短（1分钟），AI舆情分析缓存15分钟是合理的（舆情变化较慢）。

## 更新日志

### 2024-03-11
- ✅ 实现后端三层缓存机制
- ✅ 添加force_refresh参数支持
- ✅ 优化前端刷新逻辑
- ✅ 更新UI，刷新按钮添加图标
- ✅ 自动刷新默认使用缓存
- ✅ 预计节省80%以上token消耗
