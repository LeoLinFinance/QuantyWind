# 专家选择不回复功能

## 功能概述

在智者论坛中，专家大模型现在可以选择不回复某些讨论。当专家的输出包含"无需回复"关键词时，该回复将不会显示在消息框中，也不会被添加到对话历史中。

## 实现细节

### 后端实现

1. **专家分析服务** (`backend/services/expert_forum_service.py`)
   - 在 `get_expert_analysis` 方法中检查专家回复是否包含"无需回复"
   - 如果包含，返回 `None` 而不是分析结果
   - 不将该回复添加到聊天历史中

2. **API路由** (`backend/routers/expert_forum.py`)
   - 在 `/api/expert-forum/expert-analysis` 端点中处理 `None` 返回值
   - 返回 `{"skipped": True, "expert_name": "专家名称"}` 表示专家选择不回复

### 前端实现

**专家论坛页面** (`src/pages/ExpertForumPage.tsx`)
- 检查API响应中的 `skipped` 标记
- 如果为 `true`，不添加消息到界面
- 在控制台输出日志记录专家选择不回复

## 使用方法

### 在专家提示词中添加选项

在配置专家提示词时，可以添加类似以下的说明：

```
你是一位专业的投资分析师...

如果你认为当前讨论不需要你的专业意见，或者其他专家已经充分回答了问题，
你可以选择不回复。在这种情况下，请直接输出"无需回复"。
```

### 触发条件示例

专家可能选择不回复的情况：
- 讨论话题不在其专业领域内
- 其他专家已经充分回答了问题
- 当前市场情况没有需要特别关注的点
- 没有足够的信息做出专业判断

## 测试

运行测试脚本验证功能：

```bash
python3 test_expert_skip_reply.py
```

测试覆盖以下场景：
1. 正常回复 - 应该显示
2. 直接输出"无需回复" - 应该跳过
3. 回复中包含"无需回复"关键词 - 应该跳过
4. 包含类似词汇但不是"无需回复" - 应该显示

## 优势

1. **减少冗余信息** - 避免专家在不必要时发表意见
2. **提高讨论质量** - 只显示有价值的专家意见
3. **节省API调用** - 虽然API仍会被调用，但可以减少前端渲染和存储
4. **灵活性** - 专家可以根据具体情况自主决定是否参与讨论

## 注意事项

1. 关键词检测是精确匹配"无需回复"，不会误判类似词汇
2. 即使专家选择不回复，API调用仍会发生（用于判断是否需要回复）
3. 不回复的决定会在后端日志中记录，便于调试和监控
4. 前端会在控制台输出日志，但不会向用户显示任何提示

## 未来改进

可能的优化方向：
- 支持更多的不回复关键词或模式
- 在前端显示"某专家选择不参与此次讨论"的提示
- 统计专家的参与率，用于优化提示词
- 允许用户配置是否显示"不回复"的提示
