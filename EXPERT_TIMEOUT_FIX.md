# 专家模型超时问题修复

## 问题描述

专家模型在调用时经常出现超时错误，导致无法获得专家分析结果。

## 问题分析

### 原因1：缺少HTTP层超时设置

**问题**：OpenAI客户端没有设置HTTP超时参数

```python
# 修复前
client = OpenAI(
    api_key=api_key,
    base_url=self.stepfun_base_url
)
```

**影响**：
- HTTP请求可能会无限期等待
- 即使设置了asyncio超时（45秒），HTTP连接本身可能还在等待
- 导致资源浪费和不可预测的行为

### 原因2：asyncio超时时间过短

**问题**：asyncio超时设置为45秒，但没有给HTTP超时留出余地

```python
# 修复前
timeout=45.0  # 45秒超时
```

**影响**：
- 如果HTTP请求需要40秒，asyncio会在45秒时强制中断
- 没有考虑网络延迟和API处理时间
- 对于复杂查询，45秒可能不够

### 原因3：max_tokens设置过大

**问题**：max_tokens设置为2000，生成时间较长

```python
# 修复前
max_tokens=2000
```

**影响**：
- 生成2000个token需要更长时间
- 增加超时风险
- 实际上1500个token通常足够

## 修复方案

### 1. 为OpenAI客户端设置HTTP超时

**文件**：`backend/services/expert_forum_service.py`

```python
# 修复后
client = OpenAI(
    api_key=api_key,
    base_url=self.stepfun_base_url,
    timeout=60.0  # 设置60秒HTTP超时
)
```

**优势**：
- 确保HTTP请求不会无限期等待
- 提供明确的超时边界
- 便于问题诊断

### 2. 增加asyncio超时时间

**文件**：`backend/services/expert_forum_service.py`

```python
# 修复后
timeout=70.0  # 70秒超时（给HTTP 60秒超时留出余地）
```

**优势**：
- 给HTTP超时留出10秒缓冲
- 减少不必要的超时中断
- 提高成功率

### 3. 降低max_tokens

**文件**：`backend/services/expert_forum_service.py`

```python
# 修复后
max_tokens=1500  # 从2000降到1500，减少生成时间
```

**优势**：
- 减少生成时间
- 降低超时风险
- 1500个token足够表达完整的分析

### 4. 同步修复ContextSummarizer

**文件**：`backend/services/context_summarizer.py`

```python
# 修复后
self.client = OpenAI(
    api_key=self.api_key, 
    base_url=self.base_url,
    timeout=60.0  # 设置60秒HTTP超时
)
```

## 超时层级

修复后的超时控制有三个层级：

```
┌─────────────────────────────────────────┐
│ asyncio超时: 70秒                        │
│ ┌─────────────────────────────────────┐ │
│ │ HTTP超时: 60秒                       │ │
│ │ ┌─────────────────────────────────┐ │ │
│ │ │ API处理: 实际处理时间            │ │ │
│ │ └─────────────────────────────────┘ │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

1. **API处理时间**：实际的模型推理和生成时间
2. **HTTP超时（60秒）**：确保HTTP连接不会无限等待
3. **asyncio超时（70秒）**：应用层的最终超时保护

## 其他可能的超时原因

### 1. 网络问题

**症状**：
- 偶发性超时
- 某些时段超时频繁

**解决方案**：
- 检查网络连接
- 考虑使用代理
- 添加重试机制

### 2. API限流

**症状**：
- 并发调用时超时
- 返回429错误

**解决方案**：
- 已为每个专家分配独立API Key
- 添加请求间隔（前端已有2秒延迟）
- 监控API使用量

### 3. 查询过于复杂

**症状**：
- 特定查询总是超时
- 上下文很长时超时

**解决方案**：
- 优化查询内容
- 使用对话总结功能
- 限制上下文长度

### 4. 模型负载高

**症状**：
- 特定时段超时频繁
- 响应时间不稳定

**解决方案**：
- 选择负载较低的时段
- 考虑使用更快的模型
- 添加队列机制

## 测试建议

### 1. 运行诊断脚本

```bash
python3 diagnose_expert_timeout.py
```

这个脚本会测试：
- 不设置超时的情况
- 设置HTTP超时的情况
- asyncio超时控制
- 复杂查询场景

### 2. 监控日志

启动应用后，观察日志中的：
- API调用耗时
- 超时错误
- Token使用量

```bash
# 启动后端
cd backend && python3 main.py

# 观察日志
tail -f backend.log | grep -E "超时|timeout|StepFun"
```

### 3. 压力测试

同时触发多个专家分析，观察：
- 是否有超时
- 响应时间分布
- 成功率

## 预期效果

修复后应该看到：

1. **超时率降低**：从频繁超时降到偶发超时
2. **响应时间稳定**：大部分请求在30-50秒内完成
3. **成功率提高**：95%以上的请求成功
4. **日志清晰**：明确显示是HTTP超时还是asyncio超时

## 监控指标

建议监控以下指标：

1. **平均响应时间**：应该在30-40秒
2. **超时率**：应该低于5%
3. **Token使用量**：应该在1000-1500之间
4. **并发成功率**：5个专家同时调用应该都成功

## 回滚方案

如果修复后出现问题，可以回滚到之前的配置：

```python
# 回滚配置
client = OpenAI(
    api_key=api_key,
    base_url=self.stepfun_base_url
    # 不设置timeout
)

timeout=45.0  # 恢复45秒
max_tokens=2000  # 恢复2000
```

## 未来优化

1. **添加重试机制**：超时后自动重试1-2次
2. **动态超时**：根据查询复杂度调整超时时间
3. **缓存机制**：相似查询使用缓存结果
4. **流式响应**：使用streaming减少等待时间
5. **异步队列**：将专家分析放入队列异步处理
