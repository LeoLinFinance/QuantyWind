# 专家模型升级到 step-1-256k

## 升级概述

将智者论坛的专家大模型从 `step-1-128k` 升级到 `step-1-256k`，提供最大的上下文窗口支持。

## 升级详情

### 修改内容

**文件**：`backend/services/expert_forum_service.py`

```python
# 升级前
self.stepfun_model = "step-1-128k"  # 128k上下文窗口

# 升级后
self.stepfun_model = "step-1-256k"  # 256k上下文窗口
```

### 模型对比

| 特性 | step-1-128k | step-1-256k |
|------|-------------|-------------|
| 上下文窗口 | 128,000 tokens | 256,000 tokens |
| 适用场景 | 长对话 | 超长对话、文档分析 |
| 对话历史 | ~50条消息 | ~100条消息 |
| 响应速度 | 快 | 稍慢 |
| 成本 | 中等 | 较高 |

## 升级优势

### 1. 更长的对话历史

**128k模型**：
- 可以处理约50条消息的对话历史
- 适合中等长度的讨论

**256k模型**：
- 可以处理约100条消息的对话历史
- 适合长期、深入的讨论
- 几乎不会遇到上下文超限问题

### 2. 更详细的上下文

**支持更多信息**：
- 完整的对话历史
- 详细的持仓信息（10+只股票）
- 多次资讯总结
- 用户的所有问题和想法

### 3. 更好的分析质量

**更全面的分析**：
- 专家可以看到完整的讨论脉络
- 可以参考更早的对话内容
- 提供更连贯的建议
- 避免重复的建议

### 4. 减少总结需求

**更少的中断**：
- 不需要频繁触发对话总结
- 用户体验更流畅
- 保留更多的对话细节

## 使用场景

### 适合256k模型的场景

1. **长期投资讨论**
   - 跨越多天的对话
   - 持续跟踪多只股票
   - 深入的产业链分析

2. **复杂的投资组合**
   - 10+只股票的持仓
   - 详细的财务数据
   - 多维度的风险分析

3. **深度研究**
   - 行业深度分析
   - 宏观经济研究
   - 长期趋势判断

4. **教育和学习**
   - 投资知识问答
   - 策略讨论
   - 案例分析

## Token使用估算

### 典型场景（256k模型）

```
系统提示词: ~500 tokens
对话历史: ~10,000 tokens (50条消息)
持仓信息: ~2,000 tokens (10只股票)
资讯总结: ~2,000 tokens (多次总结)
用户问题: ~500 tokens
总计输入: ~15,000 tokens

专家回复: ~2,000 tokens

总计: ~17,000 tokens
```

**结论**：
- 256k模型可以轻松处理这种场景
- 还有大量余量（240k tokens）
- 几乎不会遇到上下文超限

### 极限场景

```
对话历史: ~50,000 tokens (200条消息)
持仓信息: ~5,000 tokens (20只股票)
资讯总结: ~10,000 tokens (多次详细总结)
其他上下文: ~5,000 tokens
总计输入: ~70,000 tokens

专家回复: ~2,000 tokens

总计: ~72,000 tokens
```

**结论**：
- 256k模型仍然可以处理
- 还有184k tokens余量
- 非常充裕

## 成本考虑

### Token定价估算

假设定价（实际以API提供商为准）：

| 模型 | 输入价格 | 输出价格 |
|------|---------|---------|
| step-1-32k | $0.001/1k | $0.002/1k |
| step-1-128k | $0.002/1k | $0.004/1k |
| step-1-256k | $0.003/1k | $0.006/1k |

### 成本对比

**单次请求（15k输入 + 2k输出）**：

| 模型 | 输入成本 | 输出成本 | 总成本 |
|------|---------|---------|--------|
| 128k | $0.030 | $0.008 | $0.038 |
| 256k | $0.045 | $0.012 | $0.057 |

**成本增加**：约50%

**每天100次请求**：
- 128k模型: $3.80/天 = $114/月
- 256k模型: $5.70/天 = $171/月
- 增加: $57/月

### 成本优化建议

1. **智能模型选择**
   ```python
   def select_model(context_length: int) -> str:
       if context_length < 32000:
           return "step-1-32k"  # 节省成本
       elif context_length < 128000:
           return "step-1-128k"
       else:
           return "step-1-256k"  # 只在必要时使用
   ```

2. **上下文管理**
   - 定期清理旧对话
   - 使用总结功能压缩历史
   - 只保留关键信息

3. **缓存机制**
   - 缓存相似查询的结果
   - 避免重复分析

## 性能考虑

### 响应时间

| 模型 | 平均响应时间 | 最大响应时间 |
|------|-------------|-------------|
| step-1-32k | 20-30秒 | 45秒 |
| step-1-128k | 30-40秒 | 60秒 |
| step-1-256k | 40-50秒 | 70秒 |

**注意**：
- 256k模型处理时间稍长
- 已设置70秒超时，足够使用
- 用户体验仍然可接受

### 优化建议

1. **显示进度提示**
   ```typescript
   setMessages(prev => [...prev, {
     role: 'system',
     content: '专家正在深度分析中，请稍候...'
   }])
   ```

2. **流式响应**（未来优化）
   ```python
   response = client.chat.completions.create(
       model=self.stepfun_model,
       messages=messages,
       stream=True  # 启用流式响应
   )
   ```

3. **并行处理**
   - 多个专家并行调用
   - 减少总等待时间

## 监控建议

### 1. Token使用监控

```python
# 记录每次请求的token使用
logger.info(f"Token使用: 输入={prompt_tokens}, 输出={completion_tokens}, 总计={total_tokens}")

# 统计平均token使用
avg_tokens = total_tokens_used / request_count
logger.info(f"平均token使用: {avg_tokens}")
```

### 2. 成本监控

```python
# 计算成本
input_cost = prompt_tokens * 0.003 / 1000
output_cost = completion_tokens * 0.006 / 1000
total_cost = input_cost + output_cost

logger.info(f"本次请求成本: ${total_cost:.4f}")
```

### 3. 性能监控

```python
# 记录响应时间
start_time = time.time()
response = client.chat.completions.create(...)
elapsed = time.time() - start_time

logger.info(f"响应时间: {elapsed:.2f}秒")
```

## 回滚方案

如果256k模型出现问题或成本过高，可以回滚：

### 回滚到128k

```python
self.stepfun_model = "step-1-128k"
```

### 回滚到32k

```python
self.stepfun_model = "step-1-32k"
```

同时需要：
- 启用对话总结功能
- 限制对话历史长度
- 优化上下文内容

## 测试建议

### 1. 基本功能测试

```bash
# 运行测试脚本
python3 test_quick_consult.py
```

验证：
- 专家能正常回复
- 没有上下文超限错误
- 响应时间可接受

### 2. 长对话测试

1. 发送50+条消息
2. 点击快速咨询按钮
3. 验证专家能看到完整历史

### 3. 复杂场景测试

1. 添加10+只股票到持仓
2. 接收多次资讯总结
3. 发送多条用户问题
4. 验证专家分析质量

### 4. 性能测试

1. 记录响应时间
2. 验证不超过70秒超时
3. 检查用户体验

## 最佳实践

### 1. 充分利用大上下文

**提供更多信息**：
- 详细的持仓信息
- 完整的对话历史
- 多维度的市场数据
- 用户的投资目标和风险偏好

### 2. 优化提示词

**更详细的指导**：
```python
system_prompt = """
你是一位资深投资分析师...

请参考以下信息：
1. 完整的对话历史（包含所有讨论）
2. 用户的投资目标和风险偏好
3. 当前持仓的详细信息
4. 最新的市场资讯和分析

请提供：
1. 综合分析（考虑所有历史讨论）
2. 具体建议（基于用户的完整背景）
3. 风险提示（结合历史决策）
"""
```

### 3. 保持上下文连贯

**引用历史讨论**：
- 专家可以引用之前的建议
- 跟踪建议的执行情况
- 提供连贯的投资策略

## 总结

升级到 `step-1-256k` 模型后：

✅ **优势**：
- 支持超长对话历史（100+条消息）
- 几乎不会遇到上下文超限
- 提供更全面、连贯的分析
- 更好的用户体验

⚠️ **注意**：
- 成本增加约50%
- 响应时间稍长（40-50秒）
- 需要监控token使用

💡 **建议**：
- 充分利用大上下文窗口
- 提供更详细的信息
- 监控成本和性能
- 必要时可以回滚到128k

这次升级将显著提升智者论坛的能力，让专家能够提供更深入、更连贯的投资建议！
