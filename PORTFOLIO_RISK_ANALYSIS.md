# 投资组合风险分析功能完成

## 功能概述

实现了基于真实历史数据的投资组合风险分析功能，支持用户自定义股票组合和权重配置，计算多维度风险指标。

## 核心特性

### 1. 自动数据同步机制

#### 添加股票时
- ✅ 在"市场洞察盯盘"中添加股票时，自动触发历史数据获取
- ✅ 后台异步获取5年历史数据（约1254个交易日）
- ✅ 不阻塞用户操作，数据获取在后台进行
- ✅ 获取完成后自动可用于风险分析

#### 删除股票时
- ✅ 从盯盘列表移除时，标记为"非活跃"
- ✅ 历史数据保留在数据集中（只增不删策略）
- ✅ 不再出现在投资组合配置选项中
- ✅ 不再自动更新数据

### 2. 智能股票过滤

**投资组合配置界面**：
- 只显示当前盯盘列表中的活跃股票
- 自动同步盯盘列表的变化
- 删除的股票不会出现在选项中
- 数据集中保留所有历史数据供未来使用

### 3. 投资组合配置

**用户界面**：
- 勾选要包含的股票
- 为每个股票设置权重（百分比）
- 一键归一化权重到100%
- 实时显示权重总和

**权重管理**：
- 支持手动输入权重（0-100%）
- 自动归一化功能（平均分配或按比例调整）
- 权重总和必须为100%才能计算

### 4. 风险指标计算

基于真实历史数据计算以下指标：

#### 风险度量
- **VaR (95%)**: 单日最大损失（95%置信度）
- **VaR (99%)**: 单日最大损失（99%置信度）
- **CVaR (95%)**: 条件风险价值/预期损失
- **年化波动率**: 投资组合的年化标准差
- **最大回撤**: 历史上从峰值到谷底的最大跌幅

#### 收益指标
- **年化收益率**: 预期年化回报
- **夏普比率**: 风险调整后收益（考虑总波动）
- **Sortino比率**: 下行风险调整收益（只考虑负波动）

#### 市场关系
- **Beta系数**: 相对于市场（S&P 500）的敏感度
- **相关性矩阵**: 股票之间的相关系数

#### 分布特征
- **偏度**: 收益分布的偏斜程度
- **峰度**: 极端事件发生的概率

## 技术实现

### 后端架构

#### 1. 数据服务层 (`historical_data_service.py`)
```python
- 多源数据获取（Twelve Data + Alpha Vantage）
- 增量更新机制
- 只增不删策略
- API限流保护
```

#### 2. 风险计算服务 (`risk_service.py`)
```python
- calculate_portfolio_risk(): 投资组合风险计算
- _get_returns_data(): 获取收益率数据
- _calculate_var(): VaR计算
- _calculate_cvar(): CVaR计算
- _calculate_sharpe(): 夏普比率
- _calculate_sortino(): Sortino比率
- _calculate_max_drawdown(): 最大回撤
- _calculate_beta(): Beta系数
- _calculate_correlation(): 相关性矩阵
```

#### 3. 市场服务层 (`market_service.py`)
```python
- add_stock(): 添加股票 + 自动获取历史数据
- remove_stock(): 删除股票 + 标记为非活跃
- 集成历史数据服务
```

#### 4. API端点 (`routers/risk.py`, `routers/market.py`)
```python
POST /api/portfolio-risk - 计算投资组合风险
GET /api/active-stocks - 获取活跃股票列表
POST /api/watchlist/{symbol} - 添加股票（自动获取数据）
DELETE /api/watchlist/{symbol} - 删除股票（标记非活跃）
```

### 前端实现

#### 投资组合配置面板 (`RiskAnalysisPage.tsx`)
- 股票选择（复选框）
- 权重输入（数字输入框）
- 归一化按钮
- 计算触发按钮
- 风险指标展示

#### 状态管理
```typescript
- availableStocks: 可用股票列表（只包含活跃股票）
- portfolioRisk: 计算结果
- calculatingRisk: 计算状态
```

## 使用流程

### 1. 添加股票到盯盘
```
市场洞察页面 → 搜索股票 → 添加
↓
自动触发历史数据获取（后台）
↓
数据获取完成（约8秒）
↓
可用于风险分析
```

### 2. 配置投资组合
```
风险分析页面 → 点击"💼 投资组合配置"
↓
勾选要包含的股票
↓
设置每个股票的权重
↓
点击"归一化权重"（可选）
↓
点击"📊 计算风险指标"
```

### 3. 查看风险指标
```
计算完成后显示：
- 10个核心风险指标
- 彩色卡片展示
- 风险解读说明
```

## 数据流程

### 添加股票流程
```
用户添加股票 (BABA)
    ↓
验证股票存在
    ↓
添加到盯盘列表
    ↓
触发历史数据获取
    ↓
Twelve Data API (5年数据)
    ↓
保存到数据集 (1254条记录)
    ↓
标记为活跃 (is_active: true)
    ↓
可用于投资组合配置
```

### 删除股票流程
```
用户删除股票 (BABA)
    ↓
从盯盘列表移除
    ↓
标记为非活跃 (is_active: false)
    ↓
历史数据保留
    ↓
不再出现在配置选项中
    ↓
不再自动更新
```

### 风险计算流程
```
用户配置投资组合
    ↓
验证权重总和 = 100%
    ↓
获取历史数据
    ↓
计算收益率矩阵
    ↓
计算投资组合收益率
    ↓
计算各项风险指标
    ↓
返回结果展示
```

## 测试结果

### 自动数据获取测试
```bash
# 添加BABA股票
curl -X POST http://localhost:8000/api/watchlist/BABA

# 响应
{
  "success": true,
  "symbol": "BABA",
  "name": "Alibaba Group Holding Limited",
  "message": "BABA 已添加到盯盘列表，历史数据正在后台获取"
}

# 后台日志
📥 自动获取 BABA 的历史数据...
📊 BABA 首次获取，尝试获取5年历史数据（Twelve Data）
✅ BABA 从Twelve Data成功获取 1254 条数据
✅ BABA 历史数据获取完成
```

### 投资组合风险计算测试
```bash
# 计算投资组合风险
curl -X POST http://localhost:8000/api/portfolio-risk \
  -H "Content-Type: application/json" \
  -d '{
    "portfolio": [
      {"symbol": "AAPL", "weight": 0.3},
      {"symbol": "MSFT", "weight": 0.3},
      {"symbol": "GOOGL", "weight": 0.4}
    ]
  }'

# 响应包含完整的风险指标
{
  "success": true,
  "risk_metrics": {
    "var_95": -0.0234,
    "cvar_95": -0.0312,
    "volatility": 0.2145,
    "sharpe_ratio": 1.23,
    ...
  }
}
```

## 风险指标说明

### VaR (Value at Risk)
- **含义**: 在给定置信度下，投资组合在一定时期内的最大可能损失
- **解读**: 数值越小（绝对值越大）风险越高
- **示例**: VaR(95%) = -2.34% 表示有95%的把握，单日损失不会超过2.34%

### CVaR (Conditional VaR)
- **含义**: 超过VaR阈值时的平均损失
- **解读**: 衡量极端情况下的损失程度
- **示例**: CVaR(95%) = -3.12% 表示在最坏的5%情况下，平均损失为3.12%

### 夏普比率 (Sharpe Ratio)
- **含义**: 每承担一单位风险获得的超额收益
- **解读**: 数值越大越好，>1为良好，>2为优秀
- **示例**: 1.23 表示风险调整后收益较好

### Sortino比率
- **含义**: 只考虑下行风险的风险调整收益
- **解读**: 比夏普比率更关注负面波动
- **示例**: 通常高于夏普比率

### Beta系数
- **含义**: 相对于市场的波动性
- **解读**: <1波动小于市场，>1波动大于市场
- **示例**: 0.8表示市场涨1%，组合涨0.8%

### 最大回撤
- **含义**: 历史上从峰值到谷底的最大跌幅
- **解读**: 衡量历史最坏情况
- **示例**: -15.23% 表示历史上最大跌幅为15.23%

## 注意事项

1. **数据要求**
   - 至少需要2天的历史数据才能计算
   - 建议有足够的历史数据（100天以上）以提高准确性
   - 新添加的股票需要等待数据获取完成（约8秒）

2. **权重配置**
   - 权重总和必须为100%
   - 使用"归一化权重"按钮可自动调整
   - 支持小数点后1位精度

3. **API限制**
   - Twelve Data: 8次/分钟，800次/天
   - 添加股票时会消耗1次API配额
   - 建议避免频繁添加/删除股票

4. **计算性能**
   - 基于5年历史数据（约1254个交易日）
   - 计算时间通常<1秒
   - 支持最多20个股票的组合

## 未来优化方向

1. **功能扩展**
   - 保存投资组合配置
   - 投资组合对比功能
   - 历史回测功能
   - 蒙特卡洛模拟

2. **可视化增强**
   - 风险收益散点图
   - 有效前沿曲线
   - 相关性热力图
   - 历史回撤曲线

3. **智能推荐**
   - 基于风险偏好的组合推荐
   - 最优权重计算
   - 风险预警提示

## 总结

✅ 自动数据同步机制完成
✅ 智能股票过滤实现
✅ 投资组合配置界面完成
✅ 基于真实数据的风险计算实现
✅ 10+项风险指标计算
✅ 只增不删的数据管理策略

现在用户可以：
- 在市场洞察中添加股票，自动获取历史数据
- 在风险分析中配置投资组合
- 查看基于真实数据的风险指标
- 优化投资组合配置
