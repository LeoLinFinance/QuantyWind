# 历史数据更新测试指南

## 快速测试步骤

### 1. 重启后端服务

```bash
# 停止当前运行的后端（Ctrl+C）
cd backend
python3 main.py
```

### 2. 打开浏览器

访问: http://localhost:5173

### 3. 进入风险分析看板

点击导航栏的"风险分析看板"

### 4. 打开数据集管理

点击"📊 数据集管理"按钮

### 5. 点击更新按钮

点击"🔄 更新历史数据集"按钮

### 6. 观察后端控制台输出

你应该看到类似这样的详细日志：

```
============================================================
开始更新历史数据
盯盘股票: ['AAPL', 'MSFT', 'TSLA', 'GOOGL', 'AMZN', 'NVDA', 'META', 'NFLX', 'AMD']
============================================================

📊 更新市场指数...

处理指数: ^IXIC
📥 更新 ^IXIC 从 2016-03-10 到 2026-03-10
正在获取 ^IXIC 的历史数据...
^IXIC 响应状态码: 200
✅ ^IXIC 成功获取 2500 条数据
✅ ^IXIC 更新了 2500 条数据

处理指数: ^GSPC
📥 更新 ^GSPC 从 2016-03-10 到 2026-03-10
正在获取 ^GSPC 的历史数据...
^GSPC 响应状态码: 200
✅ ^GSPC 成功获取 2500 条数据
✅ ^GSPC 更新了 2500 条数据

处理指数: ^RUT
📥 更新 ^RUT 从 2016-03-10 到 2026-03-10
正在获取 ^RUT 的历史数据...
^RUT 响应状态码: 200
✅ ^RUT 成功获取 2500 条数据
✅ ^RUT 更新了 2500 条数据

📈 更新盯盘股票...

处理股票: AAPL
📥 更新 AAPL 从 2016-03-10 到 2026-03-10
正在获取 AAPL 的历史数据...
AAPL 响应状态码: 200
✅ AAPL 成功获取 2500 条数据
✅ AAPL 更新了 2500 条数据

... (其他股票类似)

============================================================
更新完成!
成功: 12 个 - ['^IXIC', '^GSPC', '^RUT', 'AAPL', 'MSFT', 'TSLA', 'GOOGL', 'AMZN', 'NVDA', 'META', 'NFLX', 'AMD']
失败: 0 个 - []
============================================================
```

### 7. 检查前端提示

前端应该显示：
```
数据更新完成！
成功: 12个
失败: 0个
```

### 8. 查看数据集统计

数据集管理面板应该显示：
- 市场指数: 3
- 活跃股票: 9
- 总数据点数: 30000+ (约2500条/标的 × 12个标的)
- 最后更新: 刚刚的时间

## 如果看到失败

### 场景1: 全部失败（12个失败）

**可能原因**:
- 网络连接问题
- Yahoo Finance API被墙
- 防火墙阻止

**检查步骤**:
1. 测试网络连接
```bash
curl "https://query1.finance.yahoo.com/v8/finance/chart/AAPL?period1=1457568000&period2=1772928000&interval=1d"
```

2. 查看后端控制台的具体错误信息

3. 如果看到"请求超时"，可能需要VPN

### 场景2: 部分失败（如2个失败）

**可能原因**:
- 某些股票代码无效
- 某些股票已退市

**检查步骤**:
1. 查看后端控制台，找到失败的股票代码
2. 在Yahoo Finance网站上搜索该股票
3. 如果股票不存在，从盯盘列表中移除

### 场景3: 超时错误

**错误信息**:
```
❌ AAPL 请求超时
```

**解决方案**:
1. 检查网络速度
2. 重试更新
3. 如果持续超时，可能需要增加timeout参数

## 第二次测试（增量更新）

### 1. 再次点击"更新历史数据集"

### 2. 观察输出

应该看到：
```
处理指数: ^IXIC
✅ ^IXIC 数据已是最新

处理指数: ^GSPC
✅ ^GSPC 数据已是最新

... (所有标的都显示"数据已是最新")

============================================================
更新完成!
成功: 12 个
失败: 0 个
============================================================
```

### 3. 确认

- 更新速度很快（<5秒）
- 所有标的都显示"数据已是最新"
- 没有重复下载数据

## 测试增量更新

### 1. 手动修改数据文件

```bash
# 编辑数据文件，删除最后几天的数据
nano backend/data/historical/market_data.json
```

### 2. 再次更新

### 3. 观察输出

应该看到：
```
处理股票: AAPL
📥 更新 AAPL 从 2026-03-08 到 2026-03-10
正在获取 AAPL 的历史数据...
✅ AAPL 成功获取 2 条数据
✅ AAPL 更新了 2 条数据
```

### 4. 确认

- 只更新了缺失的日期
- 没有重新下载全部数据
- 增量更新机制正常工作

## 性能测试

### 首次更新（10年数据）
- 预期时间: 30-60秒
- 数据量: 约30000条（12个标的 × 2500条）
- 文件大小: 约5MB

### 日常更新（数据已最新）
- 预期时间: <5秒
- 数据量: 0条（跳过）
- 文件大小: 不变

### 增量更新（1天数据）
- 预期时间: 5-10秒
- 数据量: 约12条（12个标的 × 1条）
- 文件大小: +1.5KB

## 数据验证

### 1. 检查数据文件

```bash
# 查看文件大小
ls -lh backend/data/historical/market_data.json

# 查看数据结构
cat backend/data/historical/market_data.json | python3 -m json.tool | head -50
```

### 2. 验证数据完整性

```python
import json

with open('backend/data/historical/market_data.json', 'r') as f:
    data = json.load(f)

# 检查指数数量
print(f"指数数量: {len(data['indices'])}")

# 检查股票数量
print(f"股票数量: {len(data['stocks'])}")

# 检查AAPL的数据点数
if 'AAPL' in data['stocks']:
    print(f"AAPL数据点数: {len(data['stocks']['AAPL']['data'])}")
    print(f"AAPL最后更新: {data['stocks']['AAPL']['last_update']}")
```

### 3. 验证数据质量

```python
# 检查是否有None值
aapl_data = data['stocks']['AAPL']['data']
none_count = sum(1 for d in aapl_data if d['close'] is None)
print(f"AAPL中None值数量: {none_count}")

# 检查日期连续性
dates = [d['date'] for d in aapl_data]
print(f"日期范围: {dates[0]} 到 {dates[-1]}")
print(f"总天数: {len(dates)}")
```

## 常见问题排查

### Q1: 为什么更新很慢？

A: 首次更新需要下载10年数据，需要30-60秒。后续增量更新会很快。

### Q2: 为什么有些数据是None？

A: Yahoo Finance API在某些日期可能没有数据（如非交易日、停牌等），这是正常的。

### Q3: 可以取消正在进行的更新吗？

A: 当前版本不支持取消，需要等待完成。未来可以添加取消功能。

### Q4: 数据文件会不会太大？

A: 10只股票约5MB，100只股票约50MB，在可接受范围内。如果数据量很大，建议迁移到数据库。

### Q5: 如何清空所有数据重新开始？

A: 删除数据文件即可：
```bash
rm backend/data/historical/market_data.json
```

## 成功标志

✅ 后端控制台显示详细的处理日志
✅ 所有标的都显示"✅ 成功获取 XXX 条数据"
✅ 前端显示"成功: 12个，失败: 0个"
✅ 数据集统计信息正确更新
✅ 第二次更新显示"数据已是最新"
✅ 数据文件大小约5MB

## 失败标志

❌ 后端控制台显示"❌"错误标记
❌ 前端显示"失败: X个"
❌ 数据集统计信息没有更新
❌ 数据文件大小为0或很小

---

**测试指南版本**: v1.0
**更新时间**: 2026年3月10日
**状态**: ✅ 可用
