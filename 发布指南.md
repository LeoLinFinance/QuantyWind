# 滴答学术 - 网站发布指南

## 📋 发布前检查清单

### ✅ 当前系统状态
- 后端API: Node.js + Express + SQLite
- 前端: 纯HTML/CSS/JavaScript (demo-web/index-fixed.html)
- 数据库: SQLite (backend/data/didaxueshu.db)
- 文章数量: 49篇
- AI服务: StepFun API (阶跃星辰)

---

## 🚀 发布方案选择

### 方案一：云服务器部署（推荐）
适合：需要完整功能，包括后续AI生成新文章

**优点**：
- 完全控制
- 可以继续生成新内容
- 性能稳定

**推荐平台**：
- 阿里云ECS
- 腾讯云CVM
- AWS EC2
- DigitalOcean

**成本**：约 ¥50-200/月

---

### 方案二：Vercel + Railway（最简单）
适合：快速上线，低成本

**优点**：
- 部署简单
- 免费额度
- 自动HTTPS

**限制**：
- Vercel免费版有请求限制
- Railway免费版有使用时长限制

**成本**：免费或 $5-20/月

---

### 方案三：静态网站托管（最便宜）
适合：只展示现有49篇文章，不再生成新内容

**优点**：
- 完全免费
- 部署超简单
- 速度快

**限制**：
- 无法生成新文章
- 需要将数据转为静态JSON

**推荐平台**：
- GitHub Pages（免费）
- Netlify（免费）
- Cloudflare Pages（免费）

**成本**：完全免费

---

## 📦 方案一：云服务器部署（完整步骤）

### 1. 购买服务器
推荐配置：
- CPU: 2核
- 内存: 2GB
- 硬盘: 40GB
- 系统: Ubuntu 20.04 LTS

### 2. 服务器初始化
```bash
# 连接服务器
ssh root@your-server-ip

# 更新系统
apt update && apt upgrade -y

# 安装Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 安装PM2（进程管理器）
npm install -g pm2

# 安装Nginx（Web服务器）
apt install -y nginx

# 安装Git
apt install -y git
```

### 3. 上传项目代码
```bash
# 在服务器上创建目录
mkdir -p /var/www/didaxueshu
cd /var/www/didaxueshu

# 方式1: 使用Git（推荐）
git clone your-repo-url .

# 方式2: 使用SCP上传
# 在本地执行：
scp -r /path/to/your/project root@your-server-ip:/var/www/didaxueshu
```

### 4. 安装依赖并配置
```bash
cd /var/www/didaxueshu/backend
npm install

# 确保.env文件存在
cp .env.example .env
nano .env  # 编辑配置
```

### 5. 启动后端服务
```bash
# 使用PM2启动
pm2 start production-server.js --name didaxueshu-api

# 查看状态
pm2 status

# 查看日志
pm2 logs didaxueshu-api

# 设置开机自启
pm2 startup
pm2 save
```

### 6. 配置Nginx
```bash
# 创建Nginx配置
nano /etc/nginx/sites-available/didaxueshu
```

粘贴以下配置：
```nginx
server {
    listen 80;
    server_name your-domain.com;  # 改为你的域名

    # 前端静态文件
    location / {
        root /var/www/didaxueshu/demo-web;
        index index-fixed.html;
        try_files $uri $uri/ /index-fixed.html;
    }

    # 后端API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

启用配置：
```bash
# 创建软链接
ln -s /etc/nginx/sites-available/didaxueshu /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启Nginx
systemctl restart nginx
```

### 7. 配置域名（可选）
1. 在域名服务商添加A记录，指向服务器IP
2. 等待DNS生效（5-30分钟）

### 8. 配置HTTPS（推荐）
```bash
# 安装Certbot
apt install -y certbot python3-certbot-nginx

# 获取SSL证书
certbot --nginx -d your-domain.com

# 自动续期
certbot renew --dry-run
```

### 9. 修改前端API地址
```bash
nano /var/www/didaxueshu/demo-web/index-fixed.html
```

将 `const API = 'http://localhost:3000/api';` 改为：
```javascript
const API = 'https://your-domain.com/api';  // 或 http://your-ip/api
```

### 10. 测试访问
- 访问: http://your-domain.com 或 http://your-ip
- 检查文章是否正常加载
- 测试点击查看详情功能

---

## 🎯 方案二：Vercel + Railway 部署

### 1. 准备代码仓库
```bash
# 初始化Git（如果还没有）
git init
git add .
git commit -m "Initial commit"

# 推送到GitHub
git remote add origin your-github-repo-url
git push -u origin main
```

### 2. 部署后端到Railway

访问 https://railway.app

1. 注册/登录Railway
2. 点击 "New Project" → "Deploy from GitHub repo"
3. 选择你的仓库
4. 配置：
   - Root Directory: `backend`
   - Start Command: `node production-server.js`
5. 添加环境变量：
   - `STEPFUN_API_KEY`: 你的StepFun API密钥
6. 部署完成后，复制Railway提供的URL（如：https://xxx.railway.app）

### 3. 部署前端到Vercel

访问 https://vercel.com

1. 注册/登录Vercel
2. 点击 "Add New" → "Project"
3. 导入GitHub仓库
4. 配置：
   - Framework Preset: Other
   - Root Directory: `demo-web`
   - Build Command: 留空
   - Output Directory: `.`
5. 在部署前，修改 `demo-web/index-fixed.html`：
   ```javascript
   const API = 'https://your-railway-url.railway.app/api';
   ```
6. 点击Deploy

### 4. 测试访问
访问Vercel提供的URL（如：https://your-project.vercel.app）

---

## 💡 方案三：静态网站部署（GitHub Pages）

### 1. 生成静态数据
创建脚本 `backend/export-static.js`：
```javascript
const sqlite3 = require('sqlite3').verbose();
const fs = require('fs');
const path = require('path');

const db = new sqlite3.Database('./data/didaxueshu.db');

db.all('SELECT * FROM articles ORDER BY createdAt DESC', [], (err, rows) => {
    if (err) {
        console.error(err);
        return;
    }
    
    // 解析layers字段
    const articles = rows.map(row => ({
        ...row,
        layers: JSON.parse(row.layers)
    }));
    
    // 保存为JSON
    const outputPath = path.join(__dirname, '../demo-web/data.json');
    fs.writeFileSync(outputPath, JSON.stringify(articles, null, 2));
    
    console.log(`✅ 导出 ${articles.length} 篇文章到 ${outputPath}`);
    db.close();
});
```

运行导出：
```bash
cd backend
node export-static.js
```

### 2. 修改前端代码
修改 `demo-web/index-fixed.html`，将API调用改为读取本地JSON：

```javascript
// 替换 loadArticles 函数
async function loadArticles(cat = 'all') {
    const grid = document.getElementById('grid');
    grid.innerHTML = '<div class="loading">加载中...</div>';

    try {
        const res = await fetch('./data.json');
        const articles = await res.json();
        
        // 过滤分类
        const filtered = cat === 'all' 
            ? articles 
            : articles.filter(a => a.category === cat);
        
        if (filtered.length > 0) {
            displayArticles(filtered);
        } else {
            grid.innerHTML = '<div class="loading">暂无文章</div>';
        }
    } catch (err) {
        grid.innerHTML = '<div class="loading">加载失败</div>';
        console.error('Load error:', err);
    }
}

// 替换 openArticle 函数
async function openArticle(id) {
    console.log('🔵 打开文章:', id);
    
    try {
        const res = await fetch('./data.json');
        const articles = await res.json();
        const article = articles.find(a => a.id === id);
        
        if (article) {
            currentArticle = article;
            showModal();
        } else {
            alert('文章未找到');
        }
    } catch (err) {
        console.error('❌ 错误:', err);
        alert('加载失败');
    }
}
```

### 3. 部署到GitHub Pages

```bash
# 创建gh-pages分支
git checkout -b gh-pages

# 只保留demo-web目录的内容
git rm -rf backend miniprogram
mv demo-web/* .
rm -rf demo-web

# 提交
git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages

# 回到主分支
git checkout main
```

在GitHub仓库设置中：
1. 进入 Settings → Pages
2. Source选择 `gh-pages` 分支
3. 保存

访问: https://your-username.github.io/your-repo-name

---

## 🔧 发布后的维护

### 定期备份数据库
```bash
# 备份SQLite数据库
cp backend/data/didaxueshu.db backend/data/backup-$(date +%Y%m%d).db

# 或使用cron定时备份
crontab -e
# 添加：每天凌晨2点备份
0 2 * * * cp /var/www/didaxueshu/backend/data/didaxueshu.db /var/www/didaxueshu/backend/data/backup-$(date +\%Y\%m\%d).db
```

### 监控服务状态
```bash
# 查看PM2进程
pm2 status

# 查看日志
pm2 logs

# 重启服务
pm2 restart didaxueshu-api
```

### 生成新文章
```bash
cd /var/www/didaxueshu/backend/crawler
node collect-with-real-ai.js
```

---

## 📊 成本对比

| 方案 | 月成本 | 难度 | 功能完整度 | 推荐度 |
|------|--------|------|-----------|--------|
| 云服务器 | ¥50-200 | ⭐⭐⭐ | 100% | ⭐⭐⭐⭐⭐ |
| Vercel+Railway | $5-20 | ⭐⭐ | 100% | ⭐⭐⭐⭐ |
| GitHub Pages | 免费 | ⭐ | 80% | ⭐⭐⭐ |

---

## ❓ 常见问题

### Q: 我应该选择哪个方案？
A: 
- 如果预算充足，想要完整功能 → 云服务器
- 如果想快速上线，低成本 → Vercel + Railway
- 如果只展示现有内容，完全免费 → GitHub Pages

### Q: 域名是必须的吗？
A: 不是必须的，可以直接使用IP地址或平台提供的域名

### Q: 如何更新网站内容？
A: 
- 云服务器：SSH连接后修改文件，重启服务
- Vercel/Railway：推送代码到GitHub，自动部署
- GitHub Pages：推送到gh-pages分支

### Q: 数据库会丢失吗？
A: 定期备份即可，建议每天自动备份

---

## 📞 需要帮助？

如果在部署过程中遇到问题，请提供：
1. 选择的部署方案
2. 具体的错误信息
3. 服务器/平台配置信息
