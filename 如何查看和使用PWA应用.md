# 如何查看和使用 PWA 应用

## 📍 在哪里可以看到软件？

PWA（渐进式 Web 应用）有三种使用方式：

---

## 方式 1：在浏览器中使用（最简单）

### 步骤：

1. **启动应用**
   ```bash
   npm run dev
   ```

2. **打开浏览器**
   - 在 Chrome、Edge 或 Safari 中访问：
   - **http://localhost:3000**

3. **就可以使用了！**
   - 这就是你的应用
   - 所有功能都可以正常使用
   - 专家论坛、市场洞察、风险分析等页面都在这里

---

## 方式 2：安装到桌面（推荐）

安装后，应用会像原生软件一样出现在你的电脑上。

### Mac 上安装（Chrome/Edge）：

1. **启动应用并在浏览器中打开**
   ```bash
   npm run dev
   ```
   访问 http://localhost:3000

2. **查看地址栏右侧**
   - 会出现一个 ⊕ 或 💻 图标
   - 点击这个图标

3. **点击"安装"**
   - 会弹出安装确认对话框
   - 点击"安装"按钮

4. **安装完成！**
   - 应用会自动打开
   - 在 Dock 栏可以看到应用图标
   - 在 Applications（应用程序）文件夹也能找到

5. **下次使用**
   - 从 Launchpad 启动
   - 或从 Applications 文件夹启动
   - 或从 Dock 启动
   - 就像使用其他 Mac 应用一样！

### 安装后的位置：

- **Dock 栏**：可以固定在 Dock 上
- **Launchpad**：在 Launchpad 中可以找到
- **Applications 文件夹**：`/Applications/Chrome Apps/` 或类似位置
- **Spotlight 搜索**：搜索"量数风行"即可找到

---

## 方式 3：在手机上安装

### iOS (iPhone/iPad)：

1. 在 Safari 中访问应用网址
2. 点击底部的"分享"按钮 📤
3. 选择"添加到主屏幕"
4. 点击"添加"
5. 应用图标会出现在主屏幕上

### Android：

1. 在 Chrome 中访问应用网址
2. 会自动弹出"安装应用"横幅
3. 点击"安装"
4. 应用会添加到主屏幕和应用抽屉

---

## 🎯 快速开始

### 第一次使用：

```bash
# 1. 启动前端（在项目根目录）
npm run dev

# 2. 启动后端（新开一个终端）
cd backend
python -m uvicorn main:app --reload --port 8000
```

### 然后：

1. **浏览器访问**：http://localhost:3000
2. **看到应用界面**：
   - 顶部导航栏
   - 专家论坛、市场洞察等页面
   - 这就是你的应用！

3. **安装到桌面**（可选）：
   - 点击地址栏的安装图标
   - 或等待右下角的安装提示
   - 点击安装

---

## 📱 安装后的体验

### 桌面应用的特点：

✅ **独立窗口**
- 不显示浏览器地址栏
- 看起来像原生应用
- 有自己的应用图标

✅ **快速启动**
- 从 Dock/Launchpad 直接启动
- 不需要打开浏览器再输入网址

✅ **离线使用**
- 即使断网也能访问
- 缓存的数据仍然可用

✅ **系统集成**
- 出现在应用列表中
- 可以用 Spotlight 搜索
- 可以固定在 Dock 上

---

## 🔍 如何确认安装成功？

### 在 Mac 上：

1. **打开 Launchpad**
   - 按 F4 或用四指捏合手势
   - 搜索"量数风行"
   - 应该能看到应用图标

2. **查看 Applications 文件夹**
   - 打开 Finder
   - 进入 Applications
   - 查找 Chrome Apps 或类似文件夹
   - 应该能看到"量数风行"

3. **使用 Spotlight**
   - 按 Cmd + Space
   - 输入"量数风行"
   - 应该能搜索到应用

---

## 🎨 应用图标

当前使用的是临时 SVG 图标：
- 蓝色背景
- 白色"量数风行"文字

如果想要更专业的图标，可以：
1. 设计一个 512x512 的 PNG 图标
2. 使用在线工具生成所有尺寸：https://realfavicongenerator.net/
3. 替换 `public/` 目录中的图标文件

---

## 🐛 常见问题

### Q: 看不到安装图标？

**原因**：
- 可能已经安装过了
- 或者浏览器不支持（建议用 Chrome/Edge）

**解决**：
- 检查 Launchpad 是否已经安装
- 或等待右下角的安装提示弹出

### Q: 安装后在哪里找到应用？

**位置**：
- Mac：Launchpad、Applications 文件夹、Spotlight 搜索
- Windows：开始菜单、桌面快捷方式
- 手机：主屏幕、应用抽屉

### Q: 需要一直运行 npm run dev 吗？

**开发阶段**：是的
- 需要同时运行前端和后端服务器
- 前端：`npm run dev`（端口 3000）
- 后端：`python -m uvicorn main:app --reload --port 8000`

**生产部署**：不需要
- 构建后部署到服务器
- 用户直接访问网址即可
- 详见 `DOCKER_DEPLOYMENT.md`

---

## 🚀 下一步

### 现在就试试：

1. **运行命令**
   ```bash
   npm run dev
   ```

2. **打开浏览器**
   - 访问 http://localhost:3000

3. **查看应用**
   - 这就是你的软件！
   - 可以正常使用所有功能

4. **安装到桌面**（可选）
   - 点击地址栏的安装图标
   - 享受原生应用体验

---

## 📚 相关文档

- `PWA_READY.md` - PWA 完整说明
- `PWA_QUICK_START.md` - 快速启动指南
- `INSTALL_PWA_INSTRUCTIONS.md` - 详细安装说明
- `APP_PACKAGING_GUIDE.md` - 其他封装方案（Electron 等）

---

**总结：软件就在浏览器里！访问 http://localhost:3000 即可使用，还可以安装到桌面像原生应用一样使用。** 🎉
