# 🚀 滴答学术 - 快速部署指南

## 三种部署方案对比

| 方案 | 成本 | 难度 | 时间 | 功能 | 推荐指数 |
|------|------|------|------|------|---------|
| **GitHub Pages** | 免费 | ⭐ | 10分钟 | 静态展示 | ⭐⭐⭐⭐⭐ |
| **Vercel + Railway** | $5-20/月 | ⭐⭐ | 20分钟 | 完整功能 | ⭐⭐⭐⭐ |
| **云服务器** | ¥50-200/月 | ⭐⭐⭐ | 1小时 | 完整功能 | ⭐⭐⭐⭐ |

---

## 🎯 方案一：GitHub Pages（推荐新手）

### 优点
- ✅ 完全免费
- ✅ 部署超简单
- ✅ 自动HTTPS
- ✅ 全球CDN加速

### 限制
- ❌ 无法生成新文章（只展示现有49篇）
- ❌ 无后端API

### 部署步骤

#### 1. 导出静态数据
```bash
cd backend
node export-static.js
```

#### 2. 测试静态版本
```bash
# 方式1: 使用部署脚本（推荐）
./deploy-static.sh

# 方式2: 手动测试
cd demo-web
python3 -m http.server 8888
# 访问 http://localhost:8888/index-static.html
```

#### 3. 推送到GitHub
```bash
# 如果还没有Git仓库
git init
git add .
git commit -m "Initial commit"

# 创建GitHub仓库后
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main
git push -u origin main
```

#### 4. 配置GitHub Pages
1. 进入GitHub仓库页面
2. 点击 Settings → Pages
3. Source选择 `main` 分支
4. 目录选择 `/demo-web`
5. 保存

#### 5. 访问网站
等待1-2分钟后访问：
```
https://你的用户名.github.io/你的仓库名/index-static.html
```

---

## 🚀 方案二：Vercel + Railway

### 优点
- ✅ 部署简单
- ✅ 自动部署
- ✅ 完整功能
- ✅ 可以生成新文章

### 成本
- Vercel: 免费（有限额）
- Railway: $5/月起

### 部署步骤

#### 1. 部署后端到Railway

1. 访问 https://railway.app 并登录
2. 点击 "New Project" → "Deploy from GitHub repo"
3. 选择你的仓库
4. 配置：
   - Root Directory: `backend`
   - Start Command: `node production-server.js`
5. 添加环境变量：
   ```
   STEPFUN_API_KEY=yFhYNndvjBoIjIkFqhUQfeN16XS7IREg8pwtGLaF5LoqSrnJDTgxWY0XomNZx8Na
   ```
6. 部署完成后，复制URL（如：`https://xxx.railway.app`）

#### 2. 修改前端API地址

编辑 `demo-web/index-fixed.html`，找到：
```javascript
const API = 'http://localhost:3000/api';
```

改为：
```javascript
const API = 'https://你的railway域名.railway.app/api';
```

提交更改：
```bash
git add demo-web/index-fixed.html
git commit -m "Update API URL"
git push
```

#### 3. 部署前端到Vercel

1. 访问 https://vercel.com 并登录
2. 点击 "Add New" → "Project"
3. 导入GitHub仓库
4. 配置：
   - Framework Preset: Other
   - Root Directory: `demo-web`
   - Build Command: 留空
   - Output Directory: `.`
5. 点击 Deploy

#### 4. 访问网站
Vercel会提供一个URL：
```
https://你的项目名.vercel.app
```

---

## 🖥️ 方案三：云服务器（完全控制）

### 推荐服务商
- 阿里云ECS
- 腾讯云CVM
- AWS EC2
- DigitalOcean

### 最低配置
- CPU: 2核
- 内存: 2GB
- 硬盘: 40GB
- 系统: Ubuntu 20.04

### 快速部署脚本

连接到服务器后，运行：

```bash
# 1. 安装依赖
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo bash -
sudo apt install -y nodejs nginx git

# 2. 安装PM2
sudo npm install -g pm2

# 3. 克隆项目
cd /var/www
sudo git clone 你的仓库地址 didaxueshu
cd didaxueshu

# 4. 安装后端依赖
cd backend
npm install

# 5. 配置环境变量
cp .env.example .env
nano .env  # 编辑配置

# 6. 启动后端
pm2 start production-server.js --name didaxueshu-api
pm2 save
pm2 startup

# 7. 配置Nginx
sudo nano /etc/nginx/sites-available/didaxueshu
```

Nginx配置内容：
```nginx
server {
    listen 80;
    server_name 你的域名或IP;

    location / {
        root /var/www/didaxueshu/demo-web;
        index index-fixed.html;
        try_files $uri $uri/ /index-fixed.html;
    }

    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/didaxueshu /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

访问：`http://你的IP或域名`

---

## 🔧 配置HTTPS（可选但推荐）

### 使用Let's Encrypt免费证书

```bash
# 安装Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d 你的域名

# 自动续期
sudo certbot renew --dry-run
```

---

## 📊 我应该选择哪个方案？

### 选择GitHub Pages，如果你：
- ✅ 想要完全免费
- ✅ 只需要展示现有49篇文章
- ✅ 不需要后续生成新内容
- ✅ 想要最简单的部署

### 选择Vercel + Railway，如果你：
- ✅ 预算有限（$5-20/月）
- ✅ 需要完整功能
- ✅ 想要自动部署
- ✅ 需要后续生成新文章

### 选择云服务器，如果你：
- ✅ 需要完全控制
- ✅ 预算充足（¥50-200/月）
- ✅ 有服务器管理经验
- ✅ 需要自定义配置

---

## 🆘 常见问题

### Q: 部署后文章显示不出来？
A: 检查浏览器控制台（F12），看是否有API请求错误

### Q: GitHub Pages部署后404？
A: 确保访问的是 `/index-static.html` 而不是 `/index-fixed.html`

### Q: Railway部署失败？
A: 检查是否正确设置了Root Directory为 `backend`

### Q: 如何更新内容？
A: 
- GitHub Pages: 重新运行 `node export-static.js`，提交推送
- Vercel/Railway: 推送代码到GitHub，自动部署
- 云服务器: SSH连接，`git pull` 更新，`pm2 restart`

### Q: 如何绑定自己的域名？
A: 
- GitHub Pages: 在仓库Settings → Pages → Custom domain
- Vercel: 在项目Settings → Domains
- 云服务器: 修改Nginx配置中的 `server_name`

---

## 📞 需要帮助？

详细文档请查看：`发布指南.md`

如有问题，请提供：
1. 选择的部署方案
2. 具体错误信息
3. 浏览器控制台截图
