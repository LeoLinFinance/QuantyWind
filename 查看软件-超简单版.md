# 在哪里可以看到软件？超简单版 🎯

## 只需 2 步！

### 第 1 步：启动应用

在终端运行：

```bash
npm run dev
```

或者运行：

```bash
./启动应用.sh
```

### 第 2 步：打开浏览器

访问：**http://localhost:3000**

---

## 🎉 就这样！

你现在看到的就是你的软件！

- ✅ 顶部有导航栏
- ✅ 可以切换不同页面（专家论坛、市场洞察等）
- ✅ 所有功能都可以使用

---

## 💡 想要桌面图标？

### 在浏览器中：

1. 看地址栏右侧
2. 有个 ⊕ 或 💻 图标
3. 点击它
4. 点击"安装"

### 安装后：

- 在 **Launchpad** 可以找到
- 在 **Applications** 文件夹可以找到
- 可以固定到 **Dock** 上
- 就像其他 Mac 应用一样使用！

---

## 📍 软件在哪里？

### 方式 1：浏览器（最简单）
- 地址：http://localhost:3000
- 打开就能用

### 方式 2：桌面应用（安装后）
- 位置：Launchpad
- 位置：Applications 文件夹
- 位置：Dock（如果固定了）
- 搜索：Spotlight 搜索"量数风行"

---

## 🔥 快速启动命令

```bash
# 方法 1：直接启动
npm run dev

# 方法 2：使用脚本
./启动应用.sh
```

然后打开浏览器访问 **http://localhost:3000**

---

## ❓ 还有问题？

### "看不到安装图标"
- 可能已经安装过了
- 去 Launchpad 找找"量数风行"

### "端口被占用"
- 关闭其他占用 3000 端口的程序
- 或修改 `vite.config.ts` 中的端口号

### "页面打不开"
- 确认 `npm run dev` 正在运行
- 确认没有报错信息
- 检查是否访问了正确的地址

---

**总结：运行 `npm run dev`，然后在浏览器打开 http://localhost:3000，就能看到软件了！** 🚀
