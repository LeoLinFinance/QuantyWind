# 如何让别人的电脑也能用上？

## 🎯 三种方案对比

| 方案 | 难度 | 适用场景 | 需要时间 |
|------|------|----------|----------|
| **方案 1：局域网共享** | ⭐ 简单 | 同一 WiFi 下的设备 | 5 分钟 |
| **方案 2：云服务器部署** | ⭐⭐ 中等 | 任何人通过网址访问 | 1-2 小时 |
| **方案 3：Docker 一键部署** | ⭐⭐⭐ 较难 | 专业部署，易于管理 | 2-3 小时 |

---

## 方案 1：局域网共享（最简单）🚀

### 适用场景
- 同事在同一办公室
- 家人在同一 WiFi 下
- 临时演示给别人看

### 操作步骤

#### 1. 获取你的本机 IP 地址

```bash
# Mac/Linux
ifconfig | grep "inet " | grep -v 127.0.0.1

# 或者
ipconfig getifaddr en0
```

会显示类似：`192.168.1.100`

#### 2. 修改 Vite 配置允许外部访问

编辑 `vite.config.ts`：

```typescript
export default defineConfig({
  // ... 其他配置
  server: {
    host: '0.0.0.0',  // 允许外部访问
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

#### 3. 启动应用

```bash
npm run dev
```

#### 4. 告诉别人访问地址

别人在浏览器输入：
```
http://你的IP地址:3000
```

例如：`http://192.168.1.100:3000`

### ⚠️ 注意事项

- 只在同一 WiFi 下有效
- 你的电脑必须一直开着
- 关闭应用后别人就访问不了了
- 不适合长期使用

---

## 方案 2：云服务器部署（推荐）⭐

### 适用场景
- 任何人通过网址访问
- 长期稳定使用
- 专业的生产环境

### 需要准备
- 一台云服务器（阿里云、腾讯云、AWS 等）
- 一个域名（可选，但推荐）
- HTTPS 证书（PWA 必需）

### 快速部署步骤

#### 1. 购买云服务器

推荐配置：
- CPU：2 核
- 内存：4GB
- 系统：Ubuntu 20.04
- 带宽：5Mbps

国内推荐：
- 阿里云 ECS
- 腾讯云 CVM
- 华为云 ECS

#### 2. 连接到服务器

```bash
ssh root@你的服务器IP
```

#### 3. 安装必要软件

```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 安装 Python
apt install -y python3 python3-pip

# 安装 Nginx
apt install -y nginx

# 安装 Docker（可选）
curl -fsSL https://get.docker.com | sh
```

#### 4. 上传代码到服务器

```bash
# 在本地打包代码
tar -czf app.tar.gz .

# 上传到服务器
scp app.tar.gz root@你的服务器IP:/root/

# 在服务器上解压
ssh root@你的服务器IP
cd /root
tar -xzf app.tar.gz
```

#### 5. 构建前端

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build
```

#### 6. 配置 Nginx

创建 `/etc/nginx/sites-available/app` 文件：

```nginx
server {
    listen 80;
    server_name 你的域名或IP;

    # 前端静态文件
    location / {
        root /root/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

启用配置：

```bash
ln -s /etc/nginx/sites-available/app /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

#### 7. 启动后端

```bash
cd backend
pip3 install -r requirements.txt
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 &
```

#### 8. 配置 HTTPS（PWA 必需）

```bash
# 安装 Certbot
apt install -y certbot python3-certbot-nginx

# 获取免费 SSL 证书
certbot --nginx -d 你的域名
```

### 访问方式

- HTTP：`http://你的域名或IP`
- HTTPS：`https://你的域名`（PWA 需要）

---

## 方案 3：Docker 一键部署（最专业）🐳

### 优势
- 环境一致，不会出现"我这能跑"的问题
- 一键启动/停止
- 易于迁移和备份

### 你已经有 Docker 配置了！

项目中已经有 `docker-compose.yml`，直接使用：

#### 1. 在服务器上安装 Docker

```bash
curl -fsSL https://get.docker.com | sh
```

#### 2. 上传代码到服务器

```bash
# 本地打包
tar -czf app.tar.gz .

# 上传
scp app.tar.gz root@服务器IP:/root/
```

#### 3. 一键启动

```bash
cd /root
tar -xzf app.tar.gz

# 启动所有服务
docker-compose up -d
```

就这么简单！Docker 会自动：
- 构建前端
- 启动后端
- 配置数据库
- 设置 Nginx

#### 4. 查看状态

```bash
docker-compose ps
docker-compose logs -f
```

详细说明请查看：`DOCKER_DEPLOYMENT.md`

---

## 🎯 推荐方案选择

### 场景 1：临时演示（5 分钟）
→ **方案 1：局域网共享**
- 修改 vite.config.ts 的 host 为 '0.0.0.0'
- 启动应用
- 告诉别人你的 IP 地址

### 场景 2：正式使用（1-2 小时）
→ **方案 2：云服务器部署**
- 购买云服务器
- 按步骤部署
- 配置域名和 HTTPS
- 任何人都能访问

### 场景 3：专业部署（2-3 小时）
→ **方案 3：Docker 部署**
- 使用现有的 docker-compose.yml
- 一键启动所有服务
- 易于管理和维护

---

## 📝 快速开始指南

### 如果你想现在就让同事试用：

1. **修改 vite.config.ts**
   ```typescript
   server: {
     host: '0.0.0.0',  // 添加这行
     port: 3000,
     // ...
   }
   ```

2. **获取你的 IP**
   ```bash
   ipconfig getifaddr en0
   ```

3. **启动应用**
   ```bash
   npm run dev
   ```

4. **告诉同事访问**
   ```
   http://你的IP:3000
   ```

### 如果你想部署到云服务器：

1. **购买服务器**（阿里云/腾讯云）

2. **使用我创建的部署脚本**
   ```bash
   # 我可以帮你创建一键部署脚本
   ```

3. **配置域名和 HTTPS**

4. **分享网址给任何人**

---

## 🔧 我可以帮你做什么？

### 选项 1：创建局域网共享脚本
我可以创建一个脚本，自动：
- 获取你的 IP
- 修改配置
- 启动服务
- 显示访问地址

### 选项 2：创建云服务器部署脚本
我可以创建一个自动化部署脚本，包括：
- 环境安装
- 代码部署
- Nginx 配置
- HTTPS 设置

### 选项 3：优化 Docker 配置
优化现有的 Docker 配置，添加：
- 自动重启
- 日志管理
- 备份脚本
- 监控告警

---

## 💡 建议

### 如果是临时演示：
→ 用方案 1，5 分钟搞定

### 如果是正式使用：
→ 用方案 2 或 3，一次部署，长期使用

### 如果需要安全性：
→ 必须用 HTTPS（方案 2 或 3）
→ 参考 `SECURE_DEPLOYMENT_README.md`

---

**你想用哪个方案？我可以帮你创建对应的部署脚本！** 🚀
