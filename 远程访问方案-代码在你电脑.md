# 远程访问方案 - 代码在你电脑上跑

## 🎯 目标
- 代码和服务运行在你的电脑上
- 别人通过网址访问
- 对方只需要浏览器，不需要安装任何东西

---

## 方案对比

| 方案 | 难度 | 费用 | 速度 | 推荐度 |
|------|------|------|------|--------|
| **ngrok** | ⭐ 超简单 | 免费 | 快 | ⭐⭐⭐⭐⭐ |
| **Cloudflare Tunnel** | ⭐⭐ 简单 | 免费 | 很快 | ⭐⭐⭐⭐ |
| **frp** | ⭐⭐⭐ 中等 | 需要服务器 | 快 | ⭐⭐⭐ |
| **Tailscale** | ⭐⭐ 简单 | 免费 | 快 | ⭐⭐⭐⭐ |

---

## 🚀 方案 1：ngrok（最推荐）

### 特点
- ✅ 3 分钟搞定
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 对方只需要网址

### 步骤

#### 1. 安装 ngrok

```bash
# Mac
brew install ngrok

# 或者下载
# https://ngrok.com/download
```

#### 2. 注册账号（免费）

访问：https://dashboard.ngrok.com/signup
- 注册后获取 authtoken

#### 3. 配置 authtoken

```bash
ngrok config add-authtoken 你的token
```

#### 4. 启动你的应用

```bash
# 终端 1：启动前端
npm run dev

# 终端 2：启动后端
cd backend
python -m uvicorn main:app --reload --port 8000
```

#### 5. 启动 ngrok

```bash
# 终端 3：暴露前端
ngrok http 3000
```

#### 6. 获取公网地址

ngrok 会显示：
```
Forwarding  https://abc123.ngrok.io -> http://localhost:3000
```

#### 7. 分享给别人

告诉对方访问：`https://abc123.ngrok.io`

就这么简单！✅

### ⚠️ 注意事项

- 免费版每次启动地址会变
- 你的电脑必须开着
- 关闭 ngrok 后别人就访问不了了

---

## 🌟 方案 2：Cloudflare Tunnel（推荐）

### 特点
- ✅ 完全免费
- ✅ 速度快（Cloudflare CDN）
- ✅ 可以固定域名
- ✅ 更稳定

### 步骤

#### 1. 安装 cloudflared

```bash
# Mac
brew install cloudflare/cloudflare/cloudflared

# 或下载
# https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
```

#### 2. 登录

```bash
cloudflared tunnel login
```

会打开浏览器，登录你的 Cloudflare 账号（免费注册）

#### 3. 创建隧道

```bash
cloudflared tunnel create my-app
```

#### 4. 启动应用

```bash
# 终端 1：启动前端
npm run dev

# 终端 2：启动后端
cd backend
python -m uvicorn main:app --reload --port 8000
```

#### 5. 启动隧道

```bash
# 终端 3
cloudflared tunnel --url http://localhost:3000
```

#### 6. 获取地址

会显示类似：
```
https://abc-def-ghi.trycloudflare.com
```

#### 7. 分享给别人

告诉对方访问这个地址即可！

---

## 🔧 方案 3：Tailscale（最安全）

### 特点
- ✅ 点对点连接
- ✅ 非常安全
- ✅ 速度快
- ⚠️ 对方需要安装客户端

### 步骤

#### 1. 你和对方都安装 Tailscale

```bash
# Mac
brew install tailscale

# 或访问
# https://tailscale.com/download
```

#### 2. 你和对方都登录同一个账号

```bash
sudo tailscale up
```

#### 3. 获取你的 Tailscale IP

```bash
tailscale ip -4
```

显示类似：`100.64.1.2`

#### 4. 启动应用

```bash
npm run dev
```

#### 5. 告诉对方访问

```
http://你的Tailscale-IP:3000
```

例如：`http://100.64.1.2:3000`

---

## 📦 一键启动脚本

我可以创建一个脚本，自动：

### ngrok 版本

```bash
#!/bin/bash
# 启动应用和 ngrok

# 启动前端（后台）
npm run dev &
FRONTEND_PID=$!

# 等待前端启动
sleep 5

# 启动 ngrok
ngrok http 3000

# 清理
kill $FRONTEND_PID
```

### Cloudflare 版本

```bash
#!/bin/bash
# 启动应用和 Cloudflare Tunnel

# 启动前端（后台）
npm run dev &
FRONTEND_PID=$!

# 等待前端启动
sleep 5

# 启动隧道
cloudflared tunnel --url http://localhost:3000

# 清理
kill $FRONTEND_PID
```

---

## 🎯 推荐方案

### 如果你想最简单：
→ **用 ngrok**
- 3 分钟搞定
- 一条命令：`ngrok http 3000`
- 分享网址给别人

### 如果你想更稳定：
→ **用 Cloudflare Tunnel**
- 速度更快
- 可以固定域名
- 免费且稳定

### 如果你想最安全：
→ **用 Tailscale**
- 点对点加密
- 不经过第三方服务器
- 但对方需要安装客户端

---

## 🚀 快速开始（ngrok）

### 1. 安装 ngrok

```bash
brew install ngrok
```

### 2. 注册并配置

访问 https://dashboard.ngrok.com/signup
```bash
ngrok config add-authtoken 你的token
```

### 3. 启动应用

```bash
npm run dev
```

### 4. 在新终端启动 ngrok

```bash
ngrok http 3000
```

### 5. 复制显示的网址

```
https://abc123.ngrok-free.app
```

### 6. 发给别人

对方在浏览器打开这个网址就能用了！

---

## 💡 使用场景

### ngrok 适合：
- 临时演示
- 快速分享
- 不想配置太多

### Cloudflare Tunnel 适合：
- 长期使用
- 需要固定域名
- 追求速度

### Tailscale 适合：
- 团队内部使用
- 需要高安全性
- 对方可以安装客户端

---

## ⚠️ 重要提示

### 所有方案的共同点：

1. **你的电脑必须开着**
   - 关机后别人就访问不了

2. **网络要稳定**
   - 断网后服务会中断

3. **后端也需要暴露**
   - 如果应用需要后端 API
   - 需要同时暴露 8000 端口
   - 或者修改前端配置指向正确的后端地址

### 暴露后端的方法：

```bash
# 方法 1：再开一个 ngrok
ngrok http 8000

# 方法 2：使用 ngrok 配置文件
# 创建 ngrok.yml
tunnels:
  frontend:
    proto: http
    addr: 3000
  backend:
    proto: http
    addr: 8000

# 启动
ngrok start --all
```

---

## 🎁 我可以帮你创建什么？

### 选项 1：ngrok 一键启动脚本
- 自动启动前端和后端
- 自动启动 ngrok
- 显示访问地址

### 选项 2：Cloudflare Tunnel 配置
- 配置固定域名
- 同时暴露前后端
- 自动启动脚本

### 选项 3：完整的远程访问方案
- 包含所有工具的安装
- 一键启动所有服务
- 自动获取和显示访问地址

---

**你想用哪个方案？我可以立即帮你创建对应的脚本！** 🚀

推荐：**先试试 ngrok，3 分钟就能让别人访问！**
